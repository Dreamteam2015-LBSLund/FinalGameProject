1. soldatens inventory kollar vilka vapen som finns.  
   ```
   getCity()
   .getResources() 
   .getNoResources(Resource r)
    ```
2. spelaren väljer ett vapen
3. soldaten går och hämtar  
   ```
   character.findResource(.. 
   ```
4. soldaten är redo att attackera
